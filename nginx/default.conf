# server {
#     listen 80;
#     server_name votre-domaine.com;

#     # Proxy API (identique à votre config Caddy)
#     location /api/profil/facebook/ {
#         proxy_pass http://profil:8081/;
#         proxy_set_header Host $host;
#     }

#     location /api/ {
#         proxy_pass http://orchestre:8080/;
#         proxy_set_header Host $host;
#     }

#     # Frontend Angular - Solution SPA
#     location / {
#         proxy_pass http://webapp-interactive:4200;
#         proxy_set_header Host $host;
        
#         # Headers critiques pour Angular
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
        
#         # Fallback SPA
#         try_files $uri $uri/ /index.html;
        
#         # Cache des assets
#         location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
#             expires 1y;
#             add_header Cache-Control "public, immutable";
#         }
#     }
# }

server {
    listen 80;
    server_name localhost;

    # -----------------------------
    # API Profil
    # -----------------------------
    location /api/profil/facebook/ {
        proxy_pass http://profil:8081/;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 90;
        proxy_send_timeout 90;
    }

    # -----------------------------
    # API Orchestre
    # -----------------------------
    location /api/ {
        proxy_pass http://orchestre:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 90;
        proxy_send_timeout 90;
    }

    # -----------------------------
    # Frontend Angular (SPA)
    # -----------------------------
    location / {
        proxy_pass http://webapp-interactive:4200;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Cache des assets côté Angular
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # -----------------------------
    # Fallback SPA
    # -----------------------------
    error_page 404 /index.html;
}